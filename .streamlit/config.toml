[theme]
# Keep Streamlit's theming simple and predictable.
# Any custom styling should be additive (small CSS tweaks), not a full override.
base = "light"
primaryColor = "#3b82f6"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f8fafc"
textColor = "#0f172a"
font = "sans serif"

